// Firebase client configuration disabled - using API-only architecture
// All data operations now go through the secure server-side API layer

// Placeholder exports to maintain compatibility with existing imports
export const db = null;
export default null;

console.log('Firebase client SDK disabled - using API-only data layer for enhanced security');